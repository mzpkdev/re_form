// Mesh Tools: manifold-native ops on the loaded model — Optimize (simplify,
// smooth) and Vary (shape-changing displacement). Builds on src/lib's manifold pipeline.
export { MeshToolsPanel } from "./MeshToolsPanel"
