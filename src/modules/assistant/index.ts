// AI assistant: an in-editor chat plus its provider settings. Self-contained —
// owns its OpenRouter client (openrouter) and persisted API config (useApiConfig).
export { AssistantPanel } from "./AssistantPanel"
export { SettingsView } from "./SettingsView"
